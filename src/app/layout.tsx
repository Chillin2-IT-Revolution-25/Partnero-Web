import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Partnero - Business Collaboration Platform',
  description: 'Connect digital creators, businesses, and entrepreneurs for meaningful partnerships',
  keywords: 'business collaboration, partnerships, content creators, influencers, digital marketing',
  authors: [{ name: 'Partnero Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  )
}